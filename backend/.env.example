# Database Configuration (for local development only)
# In Lambda, these come from Secrets Manager
DB_HOST=localhost
DB_PORT=5432
DB_NAME=costcontrol
DB_USER=postgres
DB_PASSWORD=postgres

# AWS Configuration (for local development)
AWS_REGION=us-east-1
AWS_PROFILE=default

# Secrets Manager (used in Lambda)
# DATABASE_SECRET_ARN=arn:aws:secretsmanager:us-east-1:xxxx:secret:cost-control-db-credentials

# Environment
NODE_ENV=development
LOG_LEVEL=DEBUG

# Optional: Local DynamoDB endpoint
# DYNAMODB_ENDPOINT=http://localhost:8000
